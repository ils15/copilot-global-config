# 🎯 Generic Agents Reference Card\n\nQuick reference for using generic agents via `runSubagent` pattern.\n\n## 🚀 Core Agents\n\n### 1. Orchestrator\n**Role**: Main conductor coordinating all agents  \n**When**: Start here for complex features  \n**Returns**: Orchestration plan and coordination\n\n```javascript\nconst orchestration = await runSubagent({\n  agentName: 'orchestrator',\n  description: 'Orchestrate feature X',\n  prompt: 'Coordinate implementation of feature X through planning, development, review, and deployment phases'\n});\n```\n\n---\n\n### 2. Planner-Architect ⭐\n**Role**: Strategic planning, research, architecture decisions  \n**When**: Before implementation starts  \n**Returns**: TDD implementation plan with 3-10 phases  \n**Tech**: Any (research-oriented)\n\n```javascript\nconst plan = await runSubagent({\n  agentName: 'planner-architect',\n  description: 'Plan user authentication system',\n  prompt: `Plan implementation of:\n    - User registration\n    - JWT-based authentication\n    - Password reset flow\n    \n    Use TDD approach. Consider security implications.`\n});\n```\n\n**Key Questions for Planner**:\n- How should we structure this solution?\n- What are the risks and edge cases?\n- What's the optimal architecture?\n\n---\n\n### 3. Explorer 🔍\n**Role**: Rapid file discovery, pattern analysis  \n**When**: Understand existing codebase  \n**Returns**: File lists, patterns, relationships  \n**Tech**: Any language/framework  \n**Superpower**: Parallel 3-10 searches simultaneously\n\n```javascript\nconst discoveries = await Promise.all([\n  runSubagent({\n    agentName: 'explorer',\n    description: 'Find authentication files',\n    prompt: 'Locate all auth-related files: middleware, services, models,routes'\n  }),\n  runSubagent({\n    agentName: 'explorer',\n    description: 'Find user models',\n    prompt: 'Find all user entity definitions across codebase'\n  })\n]);\n```\n\n**Great for**:\n- Understanding existing code patterns\n- Finding similar implementations\n- Discovering code organization patterns\n\n---\n\n### 4. Domain-Implementer 💻 (Backend)\n**Role**: Core business logic, API endpoints, services  \n**When**: Build backend/domain features  \n**Returns**: Production-ready code with tests  \n**Tech**: FastAPI, Express, Spring, Django, Go, etc.\n\n```javascript\nconst implementation = await runSubagent({\n  agentName: 'domain-implementer',\n  description: 'Implement user API',\n  prompt: `Implement REST API endpoints:\n    - POST /users (create user with validation)\n    - GET /users/:id (fetch user)\n    - PUT /users/:id (update)\n    - DELETE /users/:id (delete)\n    \n    Use TDD. Handle errors properly. Follow plan: [plan].`\n});\n```\n\n**Architecture**:\n```\nRoutes/Handlers → Services → Database Access\n        ↓             ↓            ↓\n    (API layer)  (Logic)      (Data layer)\n```\n\n**Key Files**:\n- API routes/handlers\n- Business logic services\n- Data access repositories\n\n---\n\n### 5. UI-Implementer 🎨 (Frontend)\n**Role**: User interfaces, components, styling  \n**When**: Build UI features  \n**Returns**: Components with tests and accessibility  \n**Tech**: React, Vue, Angular, Svelte, Next.js, etc.\n\n```javascript\nconst ui = await runSubagent({\n  agentName: 'ui-implementer',\n  description: 'Build user form',\n  prompt: `Create reusable form components:\n    - LoginForm (email, password fields)\n    - RegisterForm (email, password, confirm, terms checkbox)\n    - PasswordResetForm (new password input)\n    \n    Include validation, error display, and tests.`\n});\n```\n\n**Component Structure**:\n```\nShared Components → Feature Components → Pages\n  (Atoms)            (Molecules)      (Templates)\n```\n\n**Key Files**:\n- Reusable components\n- Pages/screens\n- Hooks/state management\n- API integration\n\n---\n\n### 6. Database-Implementer 🗄️\n**Role**: Schema design, migrations, optimization  \n**When**: Data layer changes  \n**Returns**: Migration scripts with forward/reverse  \n**Tech**: PostgreSQL, MySQL, MongoDB, Firebase, etc. + any ORM\n\n```javascript\nconst migration = await runSubagent({\n  agentName: 'database-implementer',\n  description: 'Create user schema',\n  prompt: `Design/migrate database for users:\n    - users table (id, email, password_hash, created_at, updated_at)\n    - Add indexes on email and created_at\n    - Add constraints (email unique, password required)\n    - Plan for scaling`\n});\n```\n\n**Migration Pattern**:\n```\nUPGRADE: Create tables, add columns, create indexes\n↓\nVERIFY: Test data integrity\n↓\nDOWNGRADE: Reverse all changes\n```\n\n---\n\n### 7. Code-Reviewer ✅\n**Role**: Quality, correctness, security validation  \n**When**: Before merge/deploy  \n**Returns**: APPROVED / NEEDS_REVISION / FAILED + detailed findings  \n**Tech**: Any language/framework\n\n```javascript\nconst review = await runSubagent({\n  agentName: 'code-reviewer',\n  description: 'Review auth implementation',\n  prompt: `Review for:\n    - Correctness (logic, edge cases, error handling)\n    - Security (OWASP Top 10, input validation, secrets)\n    - Tests (>80% coverage)\n    - Performance (no N+1 queries, proper indexing)\n    - Accessibility (if UI)\n    \n    Return: APPROVED / NEEDS_REVISION / FAILED`\n});\n\nif (review.status === 'APPROVED') {\n  // Safe to merge/deploy\n}\n```\n\n**Checks**:\n- ✅ Logic correctness\n- ✅ Test coverage\n- ✅ Security issues\n- ✅ Performance\n- ✅ Documentation\n- ✅ Error handling\n\n---\n\n### 8. Infra-Implementer ⚙️\n**Role**: Deployment, containers, infrastructure  \n**When**: Setup deployment, create infrastructure  \n**Returns**: Container configs, deployment procedures  \n**Tech**: Docker, Kubernetes, Serverless, VMs, etc.\n\n```javascript\nconst infra = await runSubagent({\n  agentName: 'infra-implementer',\n  description: 'Create deployment config',\n  prompt: `Create Docker setup for Node.js + React + PostgreSQL:\n    - Backend Dockerfile (API service)\n    - Frontend Dockerfile (React app)\n    - docker-compose.yml (local dev)\n    - Production configuration\n    - Health checks\n    - Environment variables`\n});\n```\n\n**Components**:\n- Container images (Dockerfile)\n- Service composition (docker-compose, K8s)\n- Reverse proxy config (Nginx, Traefik)\n- Environment variables\n- Health checks\n\n---\n\n## 🔄 Orchestration Patterns\n\n### Pattern 1: Sequential Phases\n```javascript\n// Phase 1: Plan\nconst plan = await runSubagent({ agentName: 'planner-architect', ... });\n\n// Phase 2: Parallel Implementation\nconst [backend, db, ui] = await Promise.all([\n  runSubagent({ agentName: 'domain-implementer', ... }),\n  runSubagent({ agentName: 'database-implementer', ... }),\n  runSubagent({ agentName: 'ui-implementer', ... })\n]);\n\n// Phase 3: Review\nconst review = await runSubagent({ agentName: 'code-reviewer', ... });\n\n// Phase 4: Deploy\nif (review.status === 'APPROVED') {\n  await runSubagent({ agentName: 'infra-implementer', ... });\n}\n```\n\n### Pattern 2: Discovery + Plan + Implement\n```javascript\nconst discoveries = await runSubagent({ agentName: 'explorer', ... });\nconst plan = await runSubagent({ \n  agentName: 'planner-architect',\n  prompt: `Based on discoveries: ${discoveries}, create plan...`\n});\nconst impl = await runSubagent({\n  agentName: 'domain-implementer',\n  prompt: `Follow this plan: ${plan}`\n});\n```\n\n---\n\n## 🎨 Tech Stack Mapping\n\n| Layer | Python | Node.js | Java | Go |\n|-------|--------|---------|------|----|\n| **API** | FastAPI | Express | Spring | Gin |\n| **Frontend** | N/A | React/Next | N/A | N/A |\n| **Database ORM** | SQLAlchemy | Prisma | Hibernate | GORM |\n| **Testing** | pytest | Jest/Vitest | JUnit | testify |\n| **Container** | Docker | Docker | Docker | Docker |\n\n**Same agents work with any combination!**\n\n---\n\n## ✨ Tips & Tricks\n\n### ✅ DO\n- Clear, detailed prompts (explain WHY)\n- Parallel execution for independent work\n- Review before deployment\n- Use existing examples as context\n- Specific error cases in requirements\n\n### ❌ DON'T\n- Vague prompts (\"implement payment\")\n- Sequential when parallelizable\n- Deploy without review\n- Mix concerns in one request\n- Forget security considerations\n\n---\n\n## 🎓 Decision Tree\n\n```\nNeed to...\n├── Understand codebase?\n│   └─→ Explorer\n├── Plan architecture?\n│   └─→ Planner-Architect\n├── Implement logic/API?\n│   └─→ Domain-Implementer\n├── Build UI?\n│   └─→ UI-Implementer\n├── Modify database?\n│   └─→ Database-Implementer\n├── Review code?\n│   └─→ Code-Reviewer\n├── Deploy?\n│   └─→ Infra-Implementer\n└── Coordinate all?\n    └─→ Orchestrator\n```\n\n---\n\n## 📊 Output Expectations\n\n| Agent | Output | Quality |\n|-------|--------|----------|\n| Planner-Architect | Detailed plan (3-10 phases) | Production-ready |\n| Explorer | File lists + patterns | Structured findings |\n| Domain-Implementer | Code + tests | >80% test coverage |\n| UI-Implementer | Components + tests | Accessible, responsive |\n| Database-Implementer | Migration + rollback | Reversible, safe |\n| Code-Reviewer | Approval + findings | Build blocker |\n| Infra-Implementer | Configuration files | Ready to deploy |\n\n---\n\n## 🚨 Common Mistakes\n\n1. **Skipping the plan** → Leads to rework\n   - Always: Plan → Implement → Review → Deploy\n\n2. **Vague requirements** → Low quality output\n   - Be specific about requirements, edge cases, constraints\n\n3. **Review after deploy** → Can't fix easily\n   - Always: Code-Reviewer → Infra-Implementer\n\n4. **Sequential when parallel** → Slower delivery\n   - Frontend/Backend/Database can be parallel\n\n5. **Mixing concerns** → Scattered results\n   - One agent = one responsibility\n\n---\n\n## 📞 When to Use Each Agent\n\n**Explorer**: \"I don't know where the code is\"  \n**Planner**: \"What's the best way to build this?\"  \n**Domain-Implementer**: \"Build me the API\"  \n**UI-Implementer**: \"Build me the interface\"  \n**Database-Implementer**: \"Migrate the database\"  \n**Code-Reviewer**: \"Is this safe to merge?\"  \n**Infra-Implementer**: \"Deploy this\"  \n**Orchestrator**: \"Do everything in the right order\"  \n\n---\n\n**These generic agents work with ANY tech stack, language, framework, or project type!**\n